# Code Review: Phase 2.6 + Phase 2.75 — SdvBlazor + Cecil FS Redirect

**Reviewer:** general-purpose subagent (code-reviewer skill)
**Date:** 2026-07-05
**Range:** 660d0ef..d76fb8a
**Assessment:** Ready to merge: With fixes

## Strengths

- End-to-end pipeline genuinely proven (9/9 headless checks, `loadedText='Hello from VFS!'` evidence)
- Two critical Cecil fixes correctly diagnosed (TypeLoadException + MissingMethodException)
- Clean separation of concerns (SdvFileShim/Rewriter/Home.razor.cs/IVirtualFileSystem)
- Comprehensive try/catch chains with graceful fallbacks
- TrimmerRootAssembly correctly applied
- Legal posture (C4) explicitly preserved
- Tests verify IL structure correctness

## Important Issues (fix before Phase 2.8)

1. **`SdvFileShim.DirectoryExists` silently returns `true` for nonexistent paths**
   - File: `src/SdvWebPort.Vfs/SdvFileShim.cs:99-113`
   - Issue: `return true` unconditionally when EnumerateFiles doesn't throw; InMemoryVfs never throws → always-true
   - Fix: Add `DirectoryExists` to `IVirtualFileSystem`; track directories in InMemoryVfs

2. **`RewriteMethodFallback` produces broken IL (dead code, footgun)**
   - File: `src/SdvWebPort.Rewriter/SdvFileSystemRewriter.cs:188-213`
   - Issue: Uses `callee.ReturnType` → MissingMethodException if reached; plan still documents this as canonical
   - Fix: Remove or throw `InvalidOperationException`

3. **`<span id="status">` never updated — broken UX + brittle test**
   - File: `Home.razor:8` vs `Home.razor.cs` (never sets it)
   - Issue: Test waits for `status.textContent.includes('Run() returned')` which never succeeds; passes only via 8s timeout fallback
   - Fix: Wire status updates via JS interop, OR change test to wait for log signal

4. **Test coverage gaps — only 3/7 rewrite map entries tested, no runtime execution test, no SdvFileShim tests**
   - Fix: Add tests for ReadAllBytes, ReadAllText, GetFiles(2 args), DirectoryExists; add runtime execution test; add SdvFileShim unit tests

5. **`_rewriteMap` coverage too narrow for real SDV — not documented in code/README**
   - Fix: Add comment on `_rewriteMap`; update README with "Known gaps" section

6. **Type-safety gap: `File.OpenRead` returns `FileStream` but SdvFileShim returns `Stream`**
   - File: `src/MockSdv.Target/FileSystemTestGame.cs:58`
   - Issue: Works now (only Stream members used) but fragile if SDV uses FileStream-specific members
   - Fix: For Phase 2.8, consider FileStream-compatible wrapper; document limitation

## Minor Issues

1. AGENTS.md vs spec v2 inconsistency on C4 (in-memory rewriting exception)
2. Stale comment in `MockSdv.Target.csproj:7` (says net10, is net8)
3. Plan documents still show broken fallback as canonical
4. Verbose `Console.WriteLine` everywhere (add debug flag)
5. `_game` IDisposable but never disposed
6. Concurrent `TickDotNet` calls can stutter after load (add in-flight guard)
7. `SetVfs` uses lock but reads don't (asymmetry misleading)
8. `_loadAttempted = true` before load attempt (no retry)
9. No user-visible error on load failure
10. `TestTarget` in same file as tests (plan deviation undocumented)

## Recommendations

**Before Phase 2.8:** Fix Important #1-5
**For production (Phase 3+):** FileStream wrapper, Web Worker for Cecil, IDisposable, in-flight guard, debug flag, iterative _rewriteMap expansion
**Documentation:** AGENTS.md C4 nuance, stale comments, plan annotations
